# Ada Chat 交接记录（2026-02-24）

## 本次已完成

1. 供应商模型获取支持关键词检索与限量返回（防止 HF 4w+ 模型卡死）
   - 新增关键词、类型、数量上限参数
   - 后端支持 `q / model_type / limit` 并返回匹配统计
2. 增加“手动添加模型ID”入口
   - 可直接输入模型名添加到可勾选列表
3. 视频能力增强（客户端模式）
   - 支持 `文生视频 / 图生视频` 模式切换
   - 支持可选参数：时长、FPS、帧数、尺寸
   - 图生视频时校验必须上传图片
   - 结果解析支持视频 URL / task_id 展示
4. 后端视频请求增强
   - `task=video` 支持接收并透传：
     - `mode` (`text2video` / `img2video`)
     - `image`
     - `video_duration`
     - `video_fps`
     - `video_num_frames`
     - `video_size`
     - `video_width`
     - `video_height`

## 本次改动文件

- `G:\ada chat\AI.php`
- `G:\ada chat\adachat-provider-models.js`
- `G:\ada chat\script.js`
- `G:\ada chat\ai_proxy.php`

## 明天继续（待办）

1. 增加“高级 JSON 参数透传”面板（供应商私有参数可自定义）
2. 增加视频异步任务轮询（遇到 `task_id` 自动查状态）
3. 增加“仅保留图像模型”一键筛选按钮
4. 统一 i18n 文案（新增字段目前部分为中文直写）

## 续做记录（2026-02-25）

- 已完成：先做覆盖恢复检查，并恢复以下功能：
  - 模型关键词获取（关键词/类型/数量上限）
  - 手动添加模型 ID
  - 视频模式扩展（文生视频/图生视频 + 参数）
  - 视频结果解析（视频 URL / task_id）
  - 后端视频参数透传
- 已完成：今日第一步“高级 JSON 参数透传”
  - 前端新增“高级参数 JSON”开关与输入框
  - 发送前 JSON 校验
  - 构建请求时合并到请求体（保留核心字段）
  - 后端透传兜底（未知字段自动透传给供应商）
- 已完成：视频异步任务轮询（`task_id` 自动查状态）
  - 新增 `ai_proxy.php?action=video_task_status` 统一轮询代理
  - 前端在视频返回 `task_id` 时自动轮询，完成后自动回填视频 URL
  - 支持可选高级参数：`video_poll_url` / `video_status_path` / `video_poll_method` / `video_poll_body`
  - 轮询失败、超时、进行中均有消息提示
- 已完成：“仅保留图像模型”一键筛选按钮
  - 供应商模型工具栏新增按钮
  - 一键将非图像模型取消勾选，并保留图像模型
  - 支持中英文提示文案

## 防覆盖提醒（重要）

- 你的机器会自动同步仓库，建议在继续前先做一份本地备份：
  - 备份以上 4 个文件到任意安全目录
- 若同步覆盖，优先按“本次改动文件”恢复。
- 本记录文件可作为恢复参照点。

