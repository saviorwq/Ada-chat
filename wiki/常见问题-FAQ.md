# 常见问题 / FAQ

## Q: 登录失败？
A: 默认密码 `admin123`，若设置了 `ADA_LOGIN_PASSWORD` 则以环境变量为准。

## Q: 供应商或模型是空的？
A: 先新增供应商，再“获取最新模型”，最后保存模型选择。

## Q: 发消息没回复？
A: 检查 Base URL、API Key、模型类型与返回码。

## Q: 会泄露 API Key 吗？
A: 关键在部署，必须拦截 `ai_data/` `ssl/` `php/`，并启用 HTTPS。

## Q: 回答下方按钮有什么用？
A: `📋` 复制回答，`🔄` 重回答（文生图支持重回答）。
