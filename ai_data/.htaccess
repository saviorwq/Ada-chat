# 禁止直接通过 Web 访问此目录下的所有文件
# Deny all direct web access to files in this directory
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
